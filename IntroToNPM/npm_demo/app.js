//To install node packages just type "npm install <package-name>" in the terminal
//Then require("<package-name>") to a variable in your JS file.
var cat = require("cat-me"),
    knock = require("knock-knock-jokes");

console.log(cat());
console.log(knock());